
# Dịch vụ hướng kết nối và không kết nối 

- Mạng chuyển gói (datagram network) cung cấp dịch vụ hướng không kết nối tầng mạng

- Mạng mạch ảo (virtual-circuit network) cung cấp dịch vụ hướng kết nối tầng mạng

- Tương tự với các dịch vụ hướng kết nối / không kết nối TCP / UDP tầng giao vận, nhưng:
	- **Dịch vụ**: host-to-host
	- **Không lựa chọn**: Tầng mạng chỉ cung cấp hoặc dịch vụ này, hoặc dịch vụ kia
	- **Cài đặt**: Bên trong phần lõi của mạng 

# Mạch ảo (VC)

- Đây là mô hình **hướng kết nối** (connection-oriented). Trước khi gửi dữ liệu, bên gửi và bên nhận phải thiết lập một con đường logic cố định đi qua các router

- Thiết lập cuộc gọi, chia nhỏ mỗi cuộc gọi trước khi dữ liệu được truyền đi 
- Mỗi gói tin mang định danh mạch ảo (không phải là địa chỉ của host đích) 
- Mỗi router trên đường đi từ nguồn đến đích duy trì “trạng thái” cho mỗi kết nối qua
- Kết nối, các tài nguyên router (băng thông, đệm) có thể được cấp phát cho mạch ảo (Các tài nguyên dành riêng = dịch vụ dự đoán trước được)

- Một mạch ảo bao gồm:
	- **Đường** từ nguồn đến đích
	- **Số hiệu mạch ảo**, mỗi số dành cho một liên kết dọc theo đường 
	- **Các điểm đăng ký vào các bảng chuyển tiếp** trong các router dọc theo đường

- Gói thuộc về mạch ảo sẽ mang số hiệu của mạch ảo
- Số hiệu mạch ảo có thể được thay đổi trên mỗi liên kết 

- Bảng chuyển tiếp mạch ảo:
	 ![[Pasted image 20251229165943.png]]

- Mạch ảo được dùng trong ATM, frame-relay, X.25. Không được dùng trong mạng Internet ngày nay 

# Mạng chuyển gói 

- Đây là mô hình **không liên kết** (connectionless). Đây chính là cách Internet hiện nay đang vận hành

- Không có thiết lập cuộc gọi tại tầng mạng
- Các router: Không lưu giữ trạng thái về kết nối giữa các đầu cuối 
- Các gói tin được chuyển tiếp bằng các sử dụng địa chỉ host đích

- Bảng chuyển tiếp datagram
	-  ![[Pasted image 20251229170239.png]]
	- ![[Pasted image 20251229170453.png]]

- **Quy tắc khớp tiền tố dài nhất (Longest Prefix Matching):** Nếu một địa chỉ IP đích khớp với nhiều dải địa chỉ trong bảng, Router sẽ chọn dải nào **chi tiết nhất** (có số bit mạng trùng khớp nhiều nhất) để chuyển tiếp